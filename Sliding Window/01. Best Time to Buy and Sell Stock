/*
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🗓️ Pattern: Sliding Window (Dynamic / Two-Pointer)
🔢 Problem: 121. Best Time to Buy and Sell Stock
📅 Date: January 8, 2026
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🎯 Problem: 121. Best Time to Buy and Sell Stock
📌 Link: https://leetcode.com/problems/best-time-to-buy-and-sell-stock/
🏷️ Difficulty: Easy
🏆 Topics: Array, Dynamic Programming, Sliding Window

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📖 Problem Summary
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Given:
- An array `prices` where `prices[i]` is the price of a given stock on the $i^{th}$ day.

Goal:
- Maximize profit by choosing a single day to buy one stock and choosing a different day in the future to sell that stock.
- Return 0 if no profit can be made.

Key Insight:
- This is a "Sliding Window" problem where we dynamically adjust the start of our window (the buy day).
- To maximize $Profit = SellPrice - BuyPrice$, we need to keep the $BuyPrice$ as low as possible and check the profit for every subsequent $SellPrice$.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
💡 Approach (One-Pass Sliding Window)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. **Initialize**: 
   - `minVal`: Set to a very large value (infinity) to track the lowest price seen so far.
   - `maxPro`: Set to 0 to track the maximum profit found.
2. **Iterate**:
   - As we move through the array, update `minVal` if the current price is lower than our current "buy" price.
   - Calculate the potential profit at the current day: $currentPrice - minVal$.
   - Update `maxPro` if this potential profit is higher than any previously recorded profit.
3. **Result**: The window effectively slides its starting point whenever a new minimum is found.



━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
*/

class Solution {
public:
    int maxProfit(vector<int>& prices) {
        // minVal acts as the left boundary of our window (the lowest buy price)
        int minVal = 1e9; 
        int maxPro = 0;
        
        for (int p : prices) {
            // Update the window start if a cheaper buy price is found
            minVal = min(minVal, p);
            
            // Calculate profit if we sold at current price p
            // and compare it with the global maximum profit
            maxPro = max(maxPro, p - minVal);
        }
        
        return maxPro;
    }
};

/*
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
⚡ Complexity Analysis
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

⏰ Time Complexity: O(N)
   └─ We traverse the price array exactly once.
   
💾 Space Complexity: O(1)
   └─ We only use two integer variables regardless of input size.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🔍 Example Walkthrough (prices = [7, 1, 5, 3, 6, 4])
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
1. p = 7: minVal = 7, maxPro = 0.
2. p = 1: minVal = 1 (new low!), maxPro = 0.
3. p = 5: minVal = 1, maxPro = max(0, 5-1) = 4.
4. p = 3: minVal = 1, maxPro = max(4, 3-1) = 4.
5. p = 6: minVal = 1, maxPro = max(4, 6-1) = 5.
6. p = 4: minVal = 1, maxPro = max(5, 4-1) = 5.
Result: 5.
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
*/
